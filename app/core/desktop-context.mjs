import{execFileSync}from'node:child_process';
const EMPTY=Object.freeze({supported:false,process:null,title:null,source:'unavailable'});
const PS='Add-Type @"\nusing System;using System.Runtime.InteropServices;using System.Text;\npublic static class ForegroundWindow{[DllImport("user32.dll")] public static extern IntPtr GetForegroundWindow();[DllImport("user32.dll",CharSet=CharSet.Unicode)] public static extern int GetWindowText(IntPtr hWnd,StringBuilder text,int count);[DllImport("user32.dll")] public static extern uint GetWindowThreadProcessId(IntPtr hWnd,out uint processId);}\n"@; $h=[ForegroundWindow]::GetForegroundWindow(); if($h -eq [IntPtr]::Zero){'{}';exit}; $sb=New-Object Text.StringBuilder 512; [void][ForegroundWindow]::GetWindowText($h,$sb,$sb.Capacity); $pid=0; [void][ForegroundWindow]::GetWindowThreadProcessId($h,[ref]$pid); $p=Get-Process -Id $pid -ErrorAction SilentlyContinue; [pscustomobject]@{process=$p.ProcessName;title=$sb.ToString()}|ConvertTo-Json -Compress';
export class DesktopAppContext{
 snapshot(){if(process.platform!=='win32')return EMPTY;try{const raw=execFileSync('powershell.exe',['-NoProfile','-NonInteractive','-ExecutionPolicy','Bypass','-Command',PS],{encoding:'utf8',timeout:1200,windowsHide:true,maxBuffer:4096}).trim();const value=raw?JSON.parse(raw):{};return Object.freeze({supported:true,process:typeof value.process==='string'&&value.process.trim()?value.process.trim().slice(0,120):null,title:typeof value.title==='string'&&value.title.trim()?value.title.trim().slice(0,180):null,source:'windows-foreground-window'});}catch{return EMPTY;}}
}
