# Plugins

This directory contains plugin contracts, manifests, sandboxing rules, reference integrations, and plugin lifecycle code.

Plugins extend Sage only through documented capabilities and explicit permissions. They must not bypass backend policy checks, consent flows, or data access boundaries.
