# Backend

This directory contains Sage's service layer. It owns assistant orchestration, tool execution, policy enforcement, plugin coordination, and stable APIs consumed by the desktop frontend.

Backend modules should be designed around explicit contracts, observable execution, safe defaults, and deterministic handling of user permissions.
