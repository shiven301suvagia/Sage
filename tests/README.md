# Tests

This directory contains automated tests, fixtures, and quality gates for Sage.

The test strategy should cover unit behavior, module contracts, integration paths, accessibility expectations, migration safety, plugin permissions, and assistant response regressions.
