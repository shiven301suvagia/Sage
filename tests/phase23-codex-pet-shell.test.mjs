import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs/promises';import path from'node:path';import{fileURLToPath}from'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const read=p=>fs.readFile(path.join(root,p),'utf8');
test('Phase 23: pet shell is compact and self-contained',async()=>{const html=await read('app/renderer/index.html');const css=await read('app/renderer/styles.css');const source=await read('app/renderer/app.mjs');assert.match(html,/class="pet-stage"/);assert.match(html,/id="pet"/);assert.match(html,/id="bubble"/);assert.match(css,/width:112px;height:150px/);assert.match(css,/width:82px;height:138px/);assert.match(source,/pet\.addEventListener\('dblclick'/);});
test('Phase 23: pet blocks native file dragging and uses window movement',async()=>{const source=await read('app/renderer/app.mjs');const css=await read('app/renderer/styles.css');assert.match(source,/draggable=false/);assert.match(source,/dragstart/);assert.match(source,/window\.sage\.window\.moveBy/);assert.match(css,/pointer-events:none/);assert.match(css,/-webkit-user-drag:none/);});
test('Phase 23: pet has visible living states',async()=>{const css=await read('app/renderer/styles.css');assert.match(css,/emotion-sleeping/);assert.match(css,/emotion-idle/);assert.match(css,/emotion-awakening/);assert.match(css,/emotion-speaking/);assert.match(css,/emotion-thinking/);assert.match(css,/@keyframes idleFloat/);assert.match(css,/@keyframes wakeUp/);});
