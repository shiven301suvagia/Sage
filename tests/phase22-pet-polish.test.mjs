import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs/promises';import path from'node:path';import{fileURLToPath}from'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const read=p=>fs.readFile(path.join(root,p),'utf8');

test('Phase 22: pet window is deliberately minimal and transparent',async()=>{const main=await read('app/main.mjs');assert.match(main,/defaultBounds=\{width:130,height:170\}/);assert.match(main,/transparent:true/);assert.match(main,/frame:false/);assert.match(main,/alwaysOnTop:true/);});
test('Phase 22: pet presentation stays compact, animated, and non-draggable',async()=>{const css=await read('app/renderer/styles.css');const app=await read('app/renderer/app.mjs');assert.match(css,/\.pet\{[^}]*width:112px;height:150px/);assert.match(css,/\.pet #sage\{[^}]*width:82px;height:138px/);assert.match(css,/\.pet\.emotion-idle #sage\{animation:/);assert.match(css,/\.pet\.emotion-awakening #sage\{animation:/);assert.match(css,/\.pet\.emotion-speaking #sage\{animation:/);assert.match(css,/\.pet\.emotion-thinking #sage\{animation:/);assert.match(app,/sage\.draggable=false/);assert.match(app,/dragstart/);});
test('Phase 22: speech bubbles remain the lightweight companion surface',async()=>{const html=await read('app/renderer/index.html');const css=await read('app/renderer/styles.css');assert.match(html,/id="bubble" class="speech"/);assert.match(css,/\.speech\.show\{opacity:1/);});
