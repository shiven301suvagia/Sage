import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs/promises';import path from'node:path';import{fileURLToPath}from'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const read=p=>fs.readFile(path.join(root,p),'utf8');

test('Phase 22: desktop pet window is compact while chat can expand',async()=>{const main=await read('app/main.mjs');assert.match(main,/defaultBounds=\{width:190,height:230\}/);assert.match(main,/next=open\?\{width:455,height:610\}:defaultBounds/);});
test('Phase 22: pet presentation is compact, animated, and non-draggable as an image',async()=>{const css=await read('app/renderer/styles.css');const app=await read('app/renderer/app.mjs');assert.match(css,/\.pet\{[^}]*width:130px;height:190px/);assert.match(css,/\.pet #sage\{[^}]*width:98px;height:180px/);assert.match(css,/\.pet\.emotion-idle #sage\{animation:/);assert.match(css,/\.pet\.emotion-awakening #sage\{animation:/);assert.match(css,/\.pet\.emotion-speaking #sage\{animation:/);assert.match(css,/\.pet\.emotion-thinking #sage\{animation:/);assert.match(app,/sage\.draggable=false/);assert.match(app,/sage\.addEventListener\('dragstart'/);});
test('Phase 22: speech bubbles remain the lightweight companion surface',async()=>{const html=await read('app/renderer/index.html');const css=await read('app/renderer/styles.css');assert.match(html,/id="bubble" class="bubble"/);assert.match(css,/\.bubble\.show\{opacity:1/);});
