{
  "name": "sage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/tests/**/*.test.js",
    "check": "npm run build && npm test"
  },
  "devDependencies": {}
}
