{
  "name": "sage",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "main": "desktop/main.mjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/tests/**/*.test.js dist/frontend/src/**/*.test.js",
    "check": "npm run build && npm test",
    "start": "npm run build && electron .",
    "package": "npm run build && node scripts/package-release.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "electron": "37.10.3",
    "typescript": "^5.9.2"
  }
}
